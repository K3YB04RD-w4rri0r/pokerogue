# Python deps for the RL environment — the single source of truth is
# pyproject.toml; this file exists for `pip install -r` ergonomics and as the
# CI cache key. Extras:
#   pip install -e ".[train]"     # stable-baselines3 + torch
#   pip install -e ".[rendered]"  # websocket-client for the browser transport
-e .
